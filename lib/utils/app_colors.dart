import 'package:flutter/material.dart';

class AppColors {
  // 메인 브랜드 컬러 (Cyan) - 주요 버튼, 활성화 탭
  static const Color primary = Color(0xFF26C6DA);

  // 보조 컬러 (Lime) - 포인트, 분석 중 상태
  static const Color secondary = Color(0xFFDCE775);

  // 그라데이션 시작 (Lime Top)
  static const Color gradientStart = Color(0xFFD4FC79);

  // 그라데이션 끝 (Cyan Bottom)
  static const Color gradientEnd = Color(0xFF96E6A1);

  // 배경색 (Toss Style)
  static const Color background = Color(0xFFF2F4F6);

  // 텍스트 (Title)
  static const Color textBlack = Color(0xFF191F28);

  // 텍스트 (Sub)
  static const Color textGray = Color(0xFF8B95A1);
}